spring:
  application:
    name: WAY
  datasource:
    url: jdbc:mysql://localhost:3306/WAY
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  web:
    resources:
      static-locations:
        - file:/C:/Users/gram/Downloads/wayimage/
        - classpath:/static/
image:
  upload:
    dir: C:/Users/gram/Downloads/wayimage

